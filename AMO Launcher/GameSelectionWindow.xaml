<Window x:Class="AMO_Launcher.Views.GameSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AMO_Launcher.Views"
        mc:Ignorable="d"
        Title="Select F1 Game" Height="500" Width="800"
        WindowStartupLocation="CenterOwner" 
        Background="{StaticResource GeometricBackground}"
        WindowStyle="None" 
        ResizeMode="CanResize"
        AllowsTransparency="True"
        BorderThickness="0">

    <Window.Resources>
        <!-- Window Button Style -->
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1AFFFFFF" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3DFFFFFF" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource WindowButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="border" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#F1707A" TargetName="border"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid Grid.Row="0" Background="{DynamicResource AMOGoldGradientHorizontal}" Height="32" 
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,0,0,0">
                <TextBlock Text="Select Game" VerticalAlignment="Center" 
                           Foreground="Black" FontWeight="Medium" FontSize="16"/>
            </StackPanel>

            <!-- Window Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CloseButton" Content="&#xE8BB;" Style="{StaticResource CloseButtonStyle}" 
                        Click="CloseButton_Click" ToolTip="Close"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="15,0,15,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="577*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Game Icons Tray (optional) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"/>
            </Grid>

            <!-- Game List -->
            <Border Grid.Row="1" BorderThickness="1" BorderBrush="#333333" 
                    Background="{StaticResource SecondaryBrush}">
                <ListView x:Name="GameListView" Margin="0" BorderThickness="0"
                          SelectionMode="Single" Background="Transparent"
                          HorizontalContentAlignment="Stretch" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Foreground="{StaticResource TextBrush}"
                          local:GridViewColumnResizeBehavior.Stretch="True"
                          local:GridViewColumnResizeBehavior.StretchColumn="1">

                    <!-- Context Menu for Right-Click Actions -->
                    <ListView.ContextMenu>
                        <ContextMenu Style="{StaticResource ModContextMenuStyle}">
                            <MenuItem Header="Copy Path to Clipboard" 
                                      Click="CopyPathMenuItem_Click"
                                      Style="{StaticResource ModContextMenuItemStyle}">
                                <MenuItem.Icon>
                                    <TextBlock Text="📋" FontSize="14" Foreground="#FF43B2FF" />
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </ListView.ContextMenu>


                    <!-- Custom ItemContainerStyle for ListViewItems -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0" 
                                                CornerRadius="0"
                                                Padding="{TemplateBinding Padding}">
                                            <!-- Important: Use GridViewRowPresenter for proper rendering -->
                                            <GridViewRowPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#1AFD9A69" TargetName="Border"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#222222" TargetName="Border"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                            <!-- Game Name with Icon - Fixed minimum width -->
                            <GridViewColumn Header="Game" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" MinWidth="60">
                                            <!-- Game Icon -->
                                            <Border Width="36" Height="36" Background="{StaticResource PrimaryBrush}" 
                                                    CornerRadius="2" BorderBrush="#333333" BorderThickness="1" Margin="2">
                                                <Image Source="{Binding Icon}" Width="28" Height="28" 
                                                       RenderOptions.BitmapScalingMode="HighQuality"
                                                       RenderOptions.EdgeMode="Aliased"
                                                       UseLayoutRounding="True" />
                                            </Border>
                                            <!-- Game Name -->
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="10,0,0,0"
                                                       FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- Game Executable Path - Takes remaining space -->
                            <GridViewColumn Header="Game Path" Width="508">
                                <GridViewColumn.HeaderContainerStyle>
                                    <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridViewColumnHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ExecutablePath}" 
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- Default Game Indicator - Fixed width, right-aligned -->
                            <GridViewColumn Header="Default" Width="60">
                                <GridViewColumn.HeaderContainerStyle>
                                    <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridViewColumnHeaderStyle}">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="Cursor" Value="Arrow" />
                                    </Style>
                                </GridViewColumn.HeaderContainerStyle>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsDefault, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  Click="DefaultCheckBox_Click"
                                                  Tag="{Binding Id}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center">
                                            <CheckBox.Style>
                                                <Style TargetType="CheckBox">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="CheckBox">
                                                                <Grid>
                                                                    <Border x:Name="checkboxBorder" 
                                                                            BorderBrush="#444444" 
                                                                            BorderThickness="1"
                                                                            CornerRadius="2"
                                                                            Width="20" Height="20"
                                                                            Background="Transparent">
                                                                        <Path x:Name="checkMark" 
                                                                              Data="M1,5 L4,9 L9,1" 
                                                                              Stroke="{StaticResource AccentBrush}"
                                                                              StrokeThickness="2"
                                                                              Visibility="Collapsed"
                                                                              HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </Grid>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" TargetName="checkMark"/>
                                                                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}" TargetName="checkboxBorder"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" TargetName="checkboxBorder"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </CheckBox.Style>
                                        </CheckBox>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <!-- Action Buttons -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="ScanGamesButton" Content="Scan" 
                            Style="{StaticResource ActionButton}"
                            Padding="15,8" Margin="0,0,5,0"
                            Click="ScanGamesButton_Click"/>

                    <Button x:Name="AddGameButton" Content="Add" 
                            Style="{StaticResource ActionButton}"
                            Padding="15,8" Margin="0,0,5,0"
                            Click="AddGameButton_Click"/>

                    <Button x:Name="RemoveGameButton" Content="Remove" 
                            Style="{StaticResource ActionButton}"
                            Background="{StaticResource ErrorBrush}"
                            Padding="15,8" Margin="0,0,5,0"
                            Click="RemoveGameButton_Click"
                            IsEnabled="False"/>

                    <Button x:Name="MakeDefaultButton" Content="Set Default " 
                            Style="{StaticResource ActionButton}"
                            Padding="15,8" Margin="0"
                            Click="MakeDefaultButton_Click"
                            IsEnabled="False"/>
                </StackPanel>

                <!-- Cancel/Select Buttons -->
                <Button Grid.Column="4" x:Name="CancelButton" 
                        Content="Cancel" IsCancel="True"
                        Style="{StaticResource ActionButton}"
                        Background="#333333" Foreground="White"
                        Margin="5,0"
                        Click="CancelButton_Click"/>

                <Button Grid.Column="5" x:Name="SelectButton" 
                        Content="Select" IsDefault="True"
                        Style="{StaticResource PrimaryButton}"
                        Click="SelectButton_Click" 
                        Foreground="Black" FontWeight="Medium"
                        IsEnabled="False"/>
            </Grid>
        </Grid>
    </Grid>
</Window>